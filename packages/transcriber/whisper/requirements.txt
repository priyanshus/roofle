# Python mlx-whisper transcription service dependencies.
#
# Install with:
#   pip install -r requirements.txt
#
# Note: mlx-whisper requires Apple silicon (M-series). It pulls in `mlx` and
# downloads an MLX-converted Whisper model from the Hugging Face Hub on first
# use (e.g. mlx-community/whisper-large-v3-turbo). No torch/whisperx needed.

mlx-whisper>=0.4.3
numpy>=1.24
websockets>=12.0
